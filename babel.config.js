module.exports = {
  presets: [
    '@vue/cli-plugin-babel/preset',
    '@babel/preset-env' // if you're using the preset
  ],
  plugins: [
    '@babel/plugin-proposal-optional-chaining' // if you're using the plugin
  ]
}
